<div>
  <h1 class="font-bold text-4xl">Home#index</h1>
  <%= @user.each do |user| %>
    <p>
      <strong>Name:</strong>
      <%= user.email %>
    </p>

    <% end %>

      <%= link_to "New User" , new_user_registration_path %>
</div>